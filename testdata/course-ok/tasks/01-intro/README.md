# Intro task

Implement `Greet(name string) string` in `main.go` so it returns `"Hello, " + name + "!"`.

Run `go test -run TestBasic ./...` to check the basic case, and
`go test -run TestAdvanced ./...` for the edge cases.
