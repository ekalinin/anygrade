# UI screenshots

Screenshots illustrating the "ledger" design, referenced from the pull request that
introduced it. This branch carries images only and shares no history with `main`.

Captured against a live server with real data: solutions pushed over git, graded by the
local runner, and one score overridden by the teacher.
